import{q as e,s as a,B as t}from"../nitro/nitro.mjs";async function loadCameras(){try{return(await e("\n      SELECT * FROM (\n        SELECT *, ROW_NUMBER() OVER (PARTITION BY id ORDER BY timestamp DESC) as rn\n        FROM cameras\n      )\n      WHERE rn = 1 AND (is_deleted = 0 OR is_deleted IS NULL)\n      ORDER BY sort_order ASC, name ASC\n    ")||[]).map(rowToCamera)}catch{return[]}}async function saveCamera(n){const s=1e3*Date.now();await e(`\n    INSERT INTO cameras (\n      timestamp, id, name, protocol, host, port, path,\n      username, password, enabled, sort_order, is_deleted, timelapse_fps\n    ) VALUES (\n      ${s},\n      '${a(n.id)}',\n      '${a(n.name)}',\n      '${a(n.protocol)}',\n      '${a(n.host)}',\n      ${t(n.port)},\n      '${a(n.path)}',\n      '${a(n.username)}',\n      '${a(n.password)}',\n      ${n.enabled?1:0},\n      ${t(n.order)},\n      0,\n      ${n.timelapseFps||0}\n    )\n  `)}async function deleteCamera(n){const s=(await loadCameras()).find(e=>e.id===n);if(!s)return;const o=1e3*Date.now();await e(`\n    INSERT INTO cameras (\n      timestamp, id, name, protocol, host, port, path,\n      username, password, enabled, sort_order, is_deleted\n    ) VALUES (\n      ${o},\n      '${a(s.id)}',\n      '${a(s.name)}',\n      '${a(s.protocol)}',\n      '${a(s.host)}',\n      ${t(s.port)},\n      '${a(s.path)}',\n      '${a(s.username)}',\n      '${a(s.password)}',\n      0,\n      ${t(s.order)},\n      1\n    )\n  `)}function rowToCamera(e){return{id:e.id,name:e.name,protocol:e.protocol||"rtsp",host:e.host,port:e.port||554,path:e.path||"/",username:e.username||"",password:e.password||"",enabled:1===e.enabled,order:e.sort_order||0,timelapseFps:e.timelapse_fps>0?e.timelapse_fps:null}}function calculateOptimalFps(e){const a=Math.floor(1440/e),t=Math.round(a/15);return[5,10,15,24,30].reduce((e,a)=>Math.abs(a-t)<Math.abs(e-t)?a:e)}async function saveCameraFps(n,s){const o=(await loadCameras()).find(e=>e.id===n);if(!o)throw new Error("Camera not found");const r=1e3*Date.now();await e(`\n    INSERT INTO cameras (\n      timestamp, id, name, protocol, host, port, path,\n      username, password, enabled, sort_order, is_deleted, timelapse_fps\n    ) VALUES (\n      ${r},\n      '${a(o.id)}',\n      '${a(o.name)}',\n      '${a(o.protocol)}',\n      '${a(o.host)}',\n      ${t(o.port)},\n      '${a(o.path)}',\n      '${a(o.username)}',\n      '${a(o.password)}',\n      ${o.enabled?1:0},\n      ${t(o.order)},\n      0,\n      ${s}\n    )\n  `)}async function loadSchedules(t){try{return(await e(`\n      SELECT * FROM (\n        SELECT *, ROW_NUMBER() OVER (PARTITION BY id ORDER BY timestamp DESC) as rn\n        FROM camera_timelapse_schedules\n      )\n      WHERE rn = 1 AND (is_deleted = 0 OR is_deleted IS NULL)\n        AND camera_id = '${a(t)}'\n      ORDER BY timestamp DESC\n    `)||[]).map(rowToSchedule)}catch{return[]}}async function saveSchedule(n){const s=1e3*Date.now();await e(`\n    INSERT INTO camera_timelapse_schedules (\n      timestamp, id, camera_id, enabled, interval_minutes,\n      start_time, end_time, days_active, is_deleted\n    ) VALUES (\n      ${s},\n      '${a(n.id)}',\n      '${a(n.camera_id)}',\n      ${n.enabled?1:0},\n      ${t(n.interval_minutes)},\n      '${a(n.start_time)}',\n      '${a(n.end_time)}',\n      '${a(n.days_active)}',\n      0\n    )\n  `)}async function deleteSchedule(n){const s=await e(`\n    SELECT * FROM (\n      SELECT *, ROW_NUMBER() OVER (PARTITION BY id ORDER BY timestamp DESC) as rn\n      FROM camera_timelapse_schedules\n    )\n    WHERE rn = 1 AND id = '${a(n)}'\n  `);if(!s||0===s.length)return;const o=rowToSchedule(s[0]),r=1e3*Date.now();await e(`\n    INSERT INTO camera_timelapse_schedules (\n      timestamp, id, camera_id, enabled, interval_minutes,\n      start_time, end_time, days_active, is_deleted\n    ) VALUES (\n      ${r},\n      '${a(o.id)}',\n      '${a(o.camera_id)}',\n      0,\n      ${t(o.interval_minutes)},\n      '${a(o.start_time)}',\n      '${a(o.end_time)}',\n      '${a(o.days_active)}',\n      1\n    )\n  `)}function rowToSchedule(e){return{id:e.id,camera_id:e.camera_id,enabled:1===e.enabled,interval_minutes:e.interval_minutes||30,start_time:e.start_time||"",end_time:e.end_time||"",days_active:e.days_active||"all"}}const n={enabled:!1,position:"bottom-left",show_date:!0,date_format:"YYYY-MM-DD",show_time:!0,show_day_counter:!1,day_counter_start:"",day_counter_label:"Day",custom_text:"",font_size:36,text_color:"white"};async function loadOverlaySettings(t){try{const n=await e(`\n      SELECT * FROM (\n        SELECT *, ROW_NUMBER() OVER (PARTITION BY camera_id ORDER BY timestamp DESC) as rn\n        FROM camera_overlay_settings\n      )\n      WHERE rn = 1 AND (is_deleted = 0 OR is_deleted IS NULL)\n        AND camera_id = '${a(t)}'\n    `);if(n&&n.length>0)return{camera_id:(s=n[0]).camera_id,enabled:1===s.enabled,position:s.position||"bottom-left",show_date:1===s.show_date,date_format:s.date_format||"YYYY-MM-DD",show_time:1===s.show_time,show_day_counter:1===s.show_day_counter,day_counter_start:s.day_counter_start||"",day_counter_label:s.day_counter_label||"Day",custom_text:s.custom_text||"",font_size:s.font_size||36,text_color:s.text_color||"white"}}catch{}var s;return{camera_id:t,...n}}async function saveOverlaySettings(n){const s=1e3*Date.now();await e(`\n    INSERT INTO camera_overlay_settings (\n      timestamp, camera_id, enabled, position,\n      show_date, date_format, show_time,\n      show_day_counter, day_counter_start, day_counter_label,\n      custom_text, font_size, text_color, is_deleted\n    ) VALUES (\n      ${s},\n      '${a(n.camera_id)}',\n      ${n.enabled?1:0},\n      '${a(n.position)}',\n      ${n.show_date?1:0},\n      '${a(n.date_format)}',\n      ${n.show_time?1:0},\n      ${n.show_day_counter?1:0},\n      '${a(n.day_counter_start)}',\n      '${a(n.day_counter_label)}',\n      '${a(n.custom_text)}',\n      ${t(n.font_size)},\n      '${a(n.text_color)}',\n      0\n    )\n  `)}export{n as D,loadSchedules as a,saveCameraFps as b,calculateOptimalFps as c,deleteCamera as d,loadOverlaySettings as e,saveOverlaySettings as f,saveSchedule as g,deleteSchedule as h,loadCameras as l,saveCamera as s};
