import{d as t,k as o,c as s,q as n}from"../../../../../nitro/nitro.mjs";import"node:http";import"node:https";import"node:crypto";import"stream";import"events";import"http";import"crypto";import"buffer";import"zlib";import"https";import"net";import"tls";import"url";import"node:events";import"node:buffer";import"node:fs";import"node:path";import"fs";import"path";import"mqtt";import"child_process";import"pg";import"fs/promises";import"node:url";const r=t(async t=>{const r=o(t,"id");if(!r)throw s({statusCode:400,message:"Proposal id is required"});const i=await n("\n    SELECT proposal_id, title, description, nodes_json, connections_json, status\n    FROM (\n      SELECT proposal_id, title, description, nodes_json, connections_json, status, timestamp,\n             ROW_NUMBER() OVER (PARTITION BY proposal_id ORDER BY timestamp DESC) rn\n      FROM ai_proposals\n      WHERE proposal_id = $1\n    ) WHERE rn = 1\n  ",[r]).catch(()=>[]);if(!i||0===i.length)throw s({statusCode:404,message:"Proposal not found"});const p=i[0];if("pending"!==p.status)return{success:!0,proposalId:r,status:p.status,note:"Already resolved."};const esc=t=>String(t||"").replace(/'/g,"''");return await n(`\n    INSERT INTO ai_proposals (timestamp, proposal_id, title, description, nodes_json, connections_json, status)\n    VALUES (now(), '${esc(p.proposal_id)}', '${esc(p.title)}', '${esc(p.description)}',\n            '${esc(p.nodes_json||"[]")}', '${esc(p.connections_json||"[]")}', 'rejected')\n  `),{success:!0,proposalId:r,status:"rejected"}});export{r as default};
