import{d as t,a as o,q as e}from"../../../../nitro/nitro.mjs";import{g as r,b as n}from"../../../../_/lightCycle.mjs";import"node:http";import"node:https";import"node:crypto";import"stream";import"events";import"http";import"crypto";import"buffer";import"zlib";import"https";import"net";import"tls";import"url";import"node:events";import"node:buffer";import"node:fs";import"node:path";import"fs";import"path";import"mqtt";import"child_process";import"pg";import"fs/promises";import"node:url";const a=t(async t=>{var a;const i=(o(t).mac||"").replace(/[^a-zA-Z0-9]/g,""),s=new Date;let m=[];try{const t=i?`AND device_mac = '${i}'`:"";m=await e(`\n      SELECT hour(timestamp) AS h, avg(temp) AS t, count(*) AS n\n      FROM sensors_environment\n      WHERE timestamp > dateadd('d', -14, now()) AND temp IS NOT NULL ${t}\n      GROUP BY hour(timestamp)\n      ORDER BY h\n    `)}catch{m=[]}const h=m.reduce((t,o)=>t+Number(o.n||0),0);if(m.length<12||h<48){return{basis:"season",regime:"season",dataPoints:h,curve:null,...r(s)}}const d=-Math.round(s.getTimezoneOffset()/60),p=Array(24).fill(null);for(const t of m)p[Number(t.h)]=Number(t.t);const l=Array(24).fill(null);for(let t=0;t<24;t++){const o=p[t];null!=o&&(l[((t+d)%24+24)%24]=o)}const u=l.filter(t=>null!=t),c=u.reduce((t,o)=>t+o,0)/u.length,f=l.map(t=>null==t?c:t);let g=0,$=1/0,w=0,y=-1/0;for(let t=0;t<24;t++){let o=0;for(let e=0;e<12;e++)o+=null!=(a=f[(t+e)%24])?a:0;o<$&&($=o,g=t),o>y&&(y=o,w=t)}const v=Math.min(...f),b=Math.max(...f),A=$/12,M=y/12,N=`${String(g).padStart(2,"0")}:00`,O=n(N,12),S=`${String(w).padStart(2,"0")}:00`,x=n(S,12);let C,F;return b>26?(C="heat",F=`Your space reaches ${b.toFixed(1)}°C during the hot hours (${S}–${x}). Lights ON in the coolest window (${N}–${O}, ~${A.toFixed(1)}°C avg) and dark through the heat peak, so the lamp's heat isn't stacked on top of the ambient heat.`):v<18?(C="cold",F=`The coldest hours drop to ${v.toFixed(1)}°C. Lights ON during that cold window (${N}–${O}) so the lamp adds warmth when it's needed most, and dark through the milder hours (${S}–${x}).`):(C="mild",F=`Mild climate (${v.toFixed(1)}–${b.toFixed(1)}°C). Lights ON during the cooler half of the day (${N}–${O}) for a healthy day/night delta without heat stress.`),{basis:"history",regime:C,flowerOn:N,flowerOff:O,reasoning:F,dataPoints:h,days:14,minTemp:Math.round(10*v)/10,maxTemp:Math.round(10*b)/10,coolAvg:Math.round(10*A)/10,hotAvg:Math.round(10*M)/10,hotWindow:{from:S,to:x},coolWindow:{from:N,to:O},curve:f.map(t=>Math.round(10*t)/10)}});export{a as default};
