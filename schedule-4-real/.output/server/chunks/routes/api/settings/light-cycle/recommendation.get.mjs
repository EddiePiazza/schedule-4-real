import{d as t,a as o,q as e}from"../../../../nitro/nitro.mjs";import{g as r,b as a}from"../../../../_/lightCycle.mjs";import{r as i}from"../../../../_/labFlip.mjs";import"node:http";import"node:https";import"node:crypto";import"stream";import"events";import"http";import"crypto";import"buffer";import"zlib";import"https";import"net";import"tls";import"url";import"node:events";import"node:buffer";import"node:fs";import"node:path";import"fs";import"path";import"mqtt";import"child_process";import"pg";import"fs/promises";import"node:url";const n=t(async t=>{var n;const s=o(t),m=(s.mac||"").replace(/[^a-zA-Z0-9]/g,""),h=new Date,d=s.lightId?await i(s.lightId,m):null;let l=[];try{const t=m?`AND device_mac = '${m}'`:"";l=await e(`\n      SELECT hour(timestamp) AS h, avg(temp) AS t, count(*) AS n\n      FROM sensors_environment\n      WHERE timestamp > dateadd('d', -14, now()) AND temp IS NOT NULL ${t}\n      GROUP BY hour(timestamp)\n      ORDER BY h\n    `)}catch{l=[]}const p=l.reduce((t,o)=>t+Number(o.n||0),0);if(l.length<12||p<48){return{basis:"season",regime:"season",dataPoints:p,curve:null,room:d,...r(h)}}const u=-Math.round(h.getTimezoneOffset()/60),c=Array(24).fill(null);for(const t of l)c[Number(t.h)]=Number(t.t);const f=Array(24).fill(null);for(let t=0;t<24;t++){const o=c[t];null!=o&&(f[((t+u)%24+24)%24]=o)}const g=f.filter(t=>null!=t),$=g.reduce((t,o)=>t+o,0)/g.length,w=f.map(t=>null==t?$:t);let y=0,b=1/0,v=0,A=-1/0;for(let t=0;t<24;t++){let o=0;for(let e=0;e<12;e++)o+=null!=(n=w[(t+e)%24])?n:0;o<b&&(b=o,y=t),o>A&&(A=o,v=t)}const M=Math.min(...w),N=Math.max(...w),O=b/12,S=A/12,x=`${String(y).padStart(2,"0")}:00`,F=a(x,12),C=`${String(v).padStart(2,"0")}:00`,L=a(C,12);let T,E;return N>26?(T="heat",E=`Your space reaches ${N.toFixed(1)}°C during the hot hours (${C}–${L}). Lights ON in the coolest window (${x}–${F}, ~${O.toFixed(1)}°C avg) and dark through the heat peak, so the lamp's heat isn't stacked on top of the ambient heat.`):M<18?(T="cold",E=`The coldest hours drop to ${M.toFixed(1)}°C. Lights ON during that cold window (${x}–${F}) so the lamp adds warmth when it's needed most, and dark through the milder hours (${C}–${L}).`):(T="mild",E=`Mild climate (${M.toFixed(1)}–${N.toFixed(1)}°C). Lights ON during the cooler half of the day (${x}–${F}) for a healthy day/night delta without heat stress.`),{basis:"history",regime:T,flowerOn:x,flowerOff:F,reasoning:E,room:d,dataPoints:p,days:14,minTemp:Math.round(10*M)/10,maxTemp:Math.round(10*N)/10,coolAvg:Math.round(10*O)/10,hotAvg:Math.round(10*S)/10,hotWindow:{from:C,to:L},coolWindow:{from:x,to:F},curve:w.map(t=>Math.round(10*t)/10)}});export{n as default};
