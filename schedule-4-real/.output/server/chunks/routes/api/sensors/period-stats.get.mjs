import{d as m,a as i,q as a}from"../../../nitro/nitro.mjs";import"node:http";import"node:https";import"node:crypto";import"stream";import"events";import"http";import"crypto";import"buffer";import"zlib";import"https";import"net";import"tls";import"url";import"node:events";import"node:buffer";import"node:fs";import"node:path";import"fs";import"path";import"mqtt";import"child_process";import"pg";import"fs/promises";import"node:url";const t=m(async m=>{const{since:t,until:s,mac:e}=i(m);if(!t||"string"!=typeof t)return{error:'Missing or invalid "since" parameter'};try{const m=new Date(t);if(isNaN(m.getTime()))return{error:'Invalid date format for "since" parameter'};const i=m.toISOString();let o="";if(s&&"string"==typeof s){const m=new Date(s);isNaN(m.getTime())||(o=` AND timestamp < '${m.toISOString()}'`)}let n,p=`\n      SELECT\n        min(temp) as temp_min, max(temp) as temp_max, avg(temp) as temp_avg,\n        min(humi) as humi_min, max(humi) as humi_max, avg(humi) as humi_avg,\n        min(vpd) as vpd_min, max(vpd) as vpd_max, avg(vpd) as vpd_avg,\n        min(co2) as co2_min, max(co2) as co2_max, avg(co2) as co2_avg,\n        min(ppfd) as ppfd_min, max(ppfd) as ppfd_max, avg(ppfd) as ppfd_avg\n      FROM sensors_environment\n      WHERE timestamp > '${i}'${o}${e?` AND (device_mac = '${String(e).toUpperCase()}' OR device_mac = '${String(e).toLowerCase()}')`:""}\n    `;try{n=await a(p)}catch{n=await a(`\n        SELECT\n          min(temp) as temp_min, max(temp) as temp_max, avg(temp) as temp_avg,\n          min(humi) as humi_min, max(humi) as humi_max, avg(humi) as humi_avg,\n          min(vpd) as vpd_min, max(vpd) as vpd_max, avg(vpd) as vpd_avg,\n          min(co2) as co2_min, max(co2) as co2_max, avg(co2) as co2_avg\n        FROM sensors_environment\n        WHERE timestamp > '${i}'${o}${e?` AND (device_mac = '${String(e).toUpperCase()}' OR device_mac = '${String(e).toLowerCase()}')`:""}\n      `)}const[,_]=await Promise.all([Promise.resolve(n),a(`\n        SELECT\n          min(temp_soil) as temp_soil_min, max(temp_soil) as temp_soil_max, avg(temp_soil) as temp_soil_avg,\n          min(humi_soil) as humi_soil_min, max(humi_soil) as humi_soil_max, avg(humi_soil) as humi_soil_avg,\n          min(ec_soil) as ec_soil_min, max(ec_soil) as ec_soil_max, avg(ec_soil) as ec_soil_avg\n        FROM sensors_soil\n        WHERE timestamp > '${i}'${o}${e?` AND (device_mac = '${String(e).toUpperCase()}' OR device_mac = '${String(e).toLowerCase()}')`:""}\n      `).catch(()=>[])]),r={};if(n.length>0){const m=n[0];r.temp={min:m.temp_min,max:m.temp_max,avg:m.temp_avg},r.humi={min:m.humi_min,max:m.humi_max,avg:m.humi_avg},r.vpd={min:m.vpd_min,max:m.vpd_max,avg:m.vpd_avg},null!=m.co2_min&&(r.co2={min:m.co2_min,max:m.co2_max,avg:m.co2_avg}),null!=m.ppfd_min&&(r.ppfd={min:m.ppfd_min,max:m.ppfd_max,avg:m.ppfd_avg})}if(_.length>0){const m=_[0];r.temp_soil={min:m.temp_soil_min,max:m.temp_soil_max,avg:m.temp_soil_avg},r.humi_soil={min:m.humi_soil_min,max:m.humi_soil_max,avg:m.humi_soil_avg},r.ec_soil={min:m.ec_soil_min,max:m.ec_soil_max,avg:m.ec_soil_avg}}return r}catch(m){return console.error("Failed to fetch period sensor stats:",m),{}}});export{t as default};
