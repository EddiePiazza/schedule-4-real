import{d as t,D as o,B as T,c as r}from"../../../nitro/nitro.mjs";import{readFileSync as i}from"fs";import{resolve as s}from"path";import"node:http";import"node:https";import"node:crypto";import"stream";import"events";import"http";import"crypto";import"buffer";import"zlib";import"https";import"net";import"tls";import"url";import"node:events";import"node:buffer";import"node:fs";import"node:path";import"mqtt";import"child_process";import"pg";import"fs/promises";import"node:url";const e=["MQTT_HOST","MQTT_PORT","MQTT_LAN_PORT","MQTT_WS_PORT","PROXY_PORT","RELAY_PORT","QUESTDB_HOST","QUESTDB_PG_PORT","QUESTDB_HTTP_PORT","QUESTDB_ILP_PORT","DATA_RETENTION_DAYS","API_PORT","WS_PORT","APP_PASSWORD","APP_DOMAIN","RELAY_DOMAIN"],_=["API_PORT","WS_PORT","MQTT_PORT","MQTT_LAN_PORT","MQTT_WS_PORT","PROXY_PORT","RELAY_PORT","MQTT_HOST","QUESTDB_HOST","QUESTDB_PG_PORT","QUESTDB_HTTP_PORT","QUESTDB_ILP_PORT"],P=t(async t=>{const P=process.env.APP_PASSWORD||"",p=o(t,"x-auth-token")||"",m=!P||p===P;try{const t=s(T(),".env"),o=i(t,"utf-8"),r={};for(const t of o.split("\n")){const o=t.trim();if(!o||o.startsWith("#"))continue;const T=o.indexOf("=");if(-1===T)continue;const i=o.slice(0,T).trim(),s=o.slice(T+1).trim();if(e.includes(i))if("APP_PASSWORD"===i)r[i]=s?"••••••••":"";else{if(!m&&_.includes(i))continue;r[i]=s}}return r}catch(t){throw r({statusCode:500,message:"Failed to read config: "+t.message})}});export{P as default};
