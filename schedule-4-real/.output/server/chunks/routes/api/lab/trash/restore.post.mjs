import{d as e,r as t,T as a,c as n,q as l,s as o}from"../../../../nitro/nitro.mjs";import{v as s}from"../../../../_/labValidation.mjs";import"node:http";import"node:https";import"node:crypto";import"stream";import"events";import"http";import"crypto";import"buffer";import"zlib";import"https";import"net";import"tls";import"url";import"node:events";import"node:buffer";import"node:fs";import"node:path";import"fs";import"path";import"mqtt";import"child_process";import"pg";import"fs/promises";import"node:url";const i=["plant","strain","observation","harvest","pollen","hunt","breeding_project"];function toSqlValue(e){return null==e?"NULL":"number"==typeof e?String(e):"boolean"==typeof e?e?"1":"0":"string"==typeof e?`'${o(e)}'`:`'${o(JSON.stringify(e))}'`}const _=e(async e=>{const o=await t(e);s(o,{type:{type:"enum",values:i,required:!0},id:{type:"uuid",required:!0}});const _=o.type,r=a(o.id);try{switch(_){case"plant":return await async function(e){var t,a;const o=await l(`\n    SELECT * FROM (\n      SELECT *, ROW_NUMBER() OVER (PARTITION BY id ORDER BY timestamp DESC) as rn\n      FROM lab_plants\n      WHERE id = '${e}'\n    )\n    WHERE rn = 1\n  `);if(!o.length)throw n({statusCode:404,message:"Plant not found"});const s=o[0];return 1!==s.is_deleted?{already_active:!0}:(await l(`\n    INSERT INTO lab_plants (\n      timestamp, id, strain_id, seed_batch_id, zone_id, plant_code, pheno_label,\n      origin_type, parent_plant_id, project_goal, status, selection_status,\n      cull_reason, cull_date, is_keeper, keeper_notes, sex, sex_confirmed_date,\n      date_started, date_to_veg, date_to_flower, date_harvested,\n      current_week, current_day, total_veg_days, total_flower_days,\n      container_size, grow_medium, nutrients_type, training_methods,\n      final_height_cm, final_score, hof_category, hof_notes,\n      breeding_project_id, hunt_id, keeper_tier, run_count, is_deleted\n    ) VALUES (\n      now(), '${e}', ${toSqlValue(s.strain_id)}, ${toSqlValue(s.seed_batch_id)}, ${toSqlValue(s.zone_id)},\n      ${toSqlValue(s.plant_code)}, ${toSqlValue(s.pheno_label)},\n      ${toSqlValue(s.origin_type||"seed")}, ${toSqlValue(s.parent_plant_id)}, ${toSqlValue(s.project_goal)},\n      ${toSqlValue(s.status||"germinating")}, ${toSqlValue(s.selection_status||"active")},\n      ${toSqlValue(s.cull_reason)}, ${toSqlValue(s.cull_date)},\n      ${s.is_keeper?1:0}, ${toSqlValue(s.keeper_notes)}, ${toSqlValue(s.sex||"unknown")},\n      ${toSqlValue(s.sex_confirmed_date)},\n      ${toSqlValue(s.date_started)}, ${toSqlValue(s.date_to_veg)}, ${toSqlValue(s.date_to_flower)}, ${toSqlValue(s.date_harvested)},\n      ${toSqlValue(null!=(t=s.current_week)?t:1)}, ${toSqlValue(null!=(a=s.current_day)?a:1)},\n      ${toSqlValue(s.total_veg_days)}, ${toSqlValue(s.total_flower_days)},\n      ${toSqlValue(s.container_size)}, ${toSqlValue(s.grow_medium)}, ${toSqlValue(s.nutrients_type)},\n      ${toSqlValue(s.training_methods)}, ${toSqlValue(s.final_height_cm)}, ${toSqlValue(s.final_score)},\n      ${toSqlValue(s.hof_category)}, ${toSqlValue(s.hof_notes)},\n      ${toSqlValue(s.breeding_project_id)}, ${toSqlValue(s.hunt_id)},\n      ${toSqlValue(s.keeper_tier)}, ${toSqlValue(s.run_count)},\n      0\n    )\n  `),{success:!0})}(r);case"strain":return await async function(e){var t;const a=await l(`\n    SELECT * FROM (\n      SELECT *, ROW_NUMBER() OVER (PARTITION BY id ORDER BY timestamp DESC) as rn\n      FROM lab_strains\n      WHERE id = '${e}'\n    )\n    WHERE rn = 1\n  `);if(!a.length)throw n({statusCode:404,message:"Strain not found"});const o=a[0];return 1!==o.is_deleted?{already_active:!0}:(await l(`\n    INSERT INTO lab_strains (\n      timestamp, id, name, generation, lineage, breeder, seed_type,\n      indica_ratio, expected_flowering_days, expected_thc, expected_cbd,\n      dominant_terpenes, grow_difficulty, description, breeder_notes,\n      acquisition_date, acquisition_source, total_seeds, seeds_remaining,\n      is_clone_only, is_deleted\n    ) VALUES (\n      now(), '${e}', ${toSqlValue(o.name)}, ${toSqlValue(o.generation)}, ${toSqlValue(o.lineage)},\n      ${toSqlValue(o.breeder)}, ${toSqlValue(o.seed_type||"feminized")},\n      ${toSqlValue(null!=(t=o.indica_ratio)?t:50)}, ${toSqlValue(o.expected_flowering_days)},\n      ${toSqlValue(o.expected_thc)}, ${toSqlValue(o.expected_cbd)},\n      ${toSqlValue(o.dominant_terpenes)}, ${toSqlValue(o.grow_difficulty||"moderate")},\n      ${toSqlValue(o.description)}, ${toSqlValue(o.breeder_notes)},\n      ${toSqlValue(o.acquisition_date)}, ${toSqlValue(o.acquisition_source)},\n      ${toSqlValue(o.total_seeds)}, ${toSqlValue(o.seeds_remaining)},\n      ${o.is_clone_only?1:0}, 0\n    )\n  `),{success:!0})}(r);case"observation":return await async function(e){const t=await l(`\n    SELECT * FROM lab_observations WHERE id = '${e}' ORDER BY timestamp DESC LIMIT 1\n  `);if(!t.length)throw n({statusCode:404,message:"Observation not found"});const a=t[0];return 1!==a.is_deleted?{already_active:!0}:(await l(`\n    INSERT INTO lab_observations (\n      timestamp, id, plant_id, observed_at, week_number, day_number, stage, obs_type,\n      height_cm, health_score, vigor_score, pest_issues, pest_details,\n      disease_issues, disease_details, deficiency_issues, deficiency_details,\n      watering_notes, feeding_notes, training_applied, general_notes,\n      photos, checklist_data, diagnostic_data, activity_data, env_snapshot_id, batch_id, is_deleted\n    ) VALUES (\n      now(), '${e}', ${toSqlValue(a.plant_id)}, ${toSqlValue(a.observed_at)},\n      ${toSqlValue(a.week_number)}, ${toSqlValue(a.day_number)},\n      ${toSqlValue(a.stage)}, ${toSqlValue(a.obs_type||"general")},\n      ${toSqlValue(a.height_cm)}, ${toSqlValue(a.health_score)}, ${toSqlValue(a.vigor_score)},\n      ${a.pest_issues?1:0}, ${toSqlValue(a.pest_details)},\n      ${a.disease_issues?1:0}, ${toSqlValue(a.disease_details)},\n      ${a.deficiency_issues?1:0}, ${toSqlValue(a.deficiency_details)},\n      ${toSqlValue(a.watering_notes)}, ${toSqlValue(a.feeding_notes)},\n      ${toSqlValue(a.training_applied)}, ${toSqlValue(a.general_notes)},\n      ${toSqlValue(a.photos)}, ${toSqlValue(a.checklist_data)},\n      ${toSqlValue(a.diagnostic_data)}, ${toSqlValue(a.activity_data)},\n      ${toSqlValue(a.env_snapshot_id)}, ${toSqlValue(a.batch_id)},\n      0\n    )\n  `),{success:!0})}(r);case"harvest":return await async function(e){const t=await l(`\n    SELECT * FROM (\n      SELECT *, ROW_NUMBER() OVER (PARTITION BY id ORDER BY timestamp DESC) as rn\n      FROM lab_harvest_data\n      WHERE id = '${e}'\n    )\n    WHERE rn = 1\n  `);if(!t.length)throw n({statusCode:404,message:"Harvest not found"});const a=t[0];if(1!==a.is_deleted&&"__DELETED__"!==a.notes)return{already_active:!0};const o=(await l(`\n    SELECT * FROM lab_harvest_data\n    WHERE id = '${e}' AND (notes IS NULL OR notes != '__DELETED__') AND (is_deleted != 1 OR is_deleted IS NULL)\n    ORDER BY timestamp DESC\n    LIMIT 1\n  `))[0]||a;return await l(`\n    INSERT INTO lab_harvest_data (\n      timestamp, id, plant_id, harvest_date,\n      days_from_germ, days_flowering, final_height_cm,\n      wet_weight_g, dry_weight_g, trim_weight_g,\n      premium_bud_g, medium_bud_g, smalls_g,\n      yield_per_plant_g, yield_per_watt, yield_per_m2,\n      wet_dry_ratio, trichome_state, main_colas, bag_appeal_score,\n      cure_start_date, cure_duration_days,\n      cure_aroma_intensity, cure_aroma_complexity, cure_aroma_appeal,\n      cure_aroma_profile, smoke_smoothness,\n      dry_conditions, photos, notes, is_deleted, cure_log\n    ) VALUES (\n      now(), '${e}', ${toSqlValue(o.plant_id)}, ${toSqlValue(o.harvest_date)},\n      ${toSqlValue(o.days_from_germ)}, ${toSqlValue(o.days_flowering)}, ${toSqlValue(o.final_height_cm)},\n      ${toSqlValue(o.wet_weight_g)}, ${toSqlValue(o.dry_weight_g)}, ${toSqlValue(o.trim_weight_g)},\n      ${toSqlValue(o.premium_bud_g)}, ${toSqlValue(o.medium_bud_g)}, ${toSqlValue(o.smalls_g)},\n      ${toSqlValue(o.yield_per_plant_g)}, ${toSqlValue(o.yield_per_watt)}, ${toSqlValue(o.yield_per_m2)},\n      ${toSqlValue(o.wet_dry_ratio)}, ${toSqlValue(o.trichome_state)}, ${toSqlValue(o.main_colas)}, ${toSqlValue(o.bag_appeal_score)},\n      ${toSqlValue(o.cure_start_date)}, ${toSqlValue(o.cure_duration_days)},\n      ${toSqlValue(o.cure_aroma_intensity)}, ${toSqlValue(o.cure_aroma_complexity)}, ${toSqlValue(o.cure_aroma_appeal)},\n      ${toSqlValue(o.cure_aroma_profile)}, ${toSqlValue(o.smoke_smoothness)},\n      ${toSqlValue(o.dry_conditions)}, ${toSqlValue(o.photos)},\n      ${o.notes&&"__DELETED__"!==o.notes?toSqlValue(o.notes):"NULL"},\n      0,\n      ${toSqlValue(o.cure_log)}\n    )\n  `),{success:!0}}(r);case"pollen":return await async function(e){var t,a,o;const s=await l(`\n    SELECT * FROM (\n      SELECT *, ROW_NUMBER() OVER (PARTITION BY id ORDER BY timestamp DESC) as rn\n      FROM lab_pollen_storage\n      WHERE id = '${e}'\n    )\n    WHERE rn = 1\n  `);if(!s.length)throw n({statusCode:404,message:"Pollen record not found"});const i=s[0];if(1!==i.is_deleted&&"depleted"!==i.status)return{already_active:!0};const _=(await l(`\n    SELECT * FROM lab_pollen_storage\n    WHERE id = '${e}' AND (status != 'depleted' OR status IS NULL) AND (is_deleted != 1 OR is_deleted IS NULL)\n    ORDER BY timestamp DESC\n    LIMIT 1\n  `))[0]||i;return await l(`\n    INSERT INTO lab_pollen_storage (\n      timestamp, id, male_plant_id, strain_id, batch_code,\n      collection_date, quantity_grams, viability_at_collection,\n      last_viability_test, current_viability,\n      storage_method, storage_location, desiccant_type, container_type,\n      status, notes, is_deleted\n    ) VALUES (\n      now(), '${e}',\n      ${toSqlValue(_.male_plant_id)}, ${toSqlValue(_.strain_id)},\n      ${toSqlValue(_.batch_code)}, ${toSqlValue(_.collection_date)},\n      ${toSqlValue(null!=(t=_.quantity_grams)?t:0)}, ${toSqlValue(null!=(a=_.viability_at_collection)?a:100)},\n      ${toSqlValue(_.last_viability_test)}, ${toSqlValue(null!=(o=_.current_viability)?o:100)},\n      ${toSqlValue(_.storage_method||"refrigerated")}, ${toSqlValue(_.storage_location)},\n      ${toSqlValue(_.desiccant_type)}, ${toSqlValue(_.container_type)},\n      ${toSqlValue("available")}, ${toSqlValue(_.notes)}, 0\n    )\n  `),{success:!0}}(r);case"hunt":return await async function(e){var t;const a=await l(`\n    SELECT * FROM (\n      SELECT *, ROW_NUMBER() OVER (PARTITION BY id ORDER BY timestamp DESC) as rn\n      FROM lab_hunts\n      WHERE id = '${e}'\n    )\n    WHERE rn = 1\n  `);if(!a.length)throw n({statusCode:404,message:"Hunt not found"});const o=a[0];return 1!==o.is_deleted?{already_active:!0}:(await l(`\n    INSERT INTO lab_hunts (\n      timestamp, id, name, strain_id, room_id, population_size, status,\n      males, checkpoints, pressure_events, weekly_checklists, carb_allocation_data,\n      started_at, completed_at, notes, is_deleted\n    ) VALUES (\n      now(), '${e}', ${toSqlValue(o.name)}, ${toSqlValue(o.strain_id)}, ${toSqlValue(o.room_id)},\n      ${toSqlValue(null!=(t=o.population_size)?t:0)}, ${toSqlValue(o.status||"setup")},\n      ${toSqlValue(o.males||"[]")}, ${toSqlValue(o.checkpoints||"[]")},\n      ${toSqlValue(o.pressure_events||"[]")}, ${toSqlValue(o.weekly_checklists||"{}")},\n      ${toSqlValue(o.carb_allocation_data||"{}")},\n      ${toSqlValue(o.started_at)}, ${toSqlValue(o.completed_at)},\n      ${toSqlValue(o.notes)}, 0\n    )\n  `),{success:!0})}(r);case"breeding_project":return await async function(e){var t,a,o;const s=await l(`\n    SELECT * FROM (\n      SELECT *, ROW_NUMBER() OVER (PARTITION BY id ORDER BY timestamp DESC) as rn\n      FROM lab_breeding_projects\n      WHERE id = '${e}'\n    )\n    WHERE rn = 1\n  `);if(!s.length)throw n({statusCode:404,message:"Breeding project not found"});const i=s[0];return 1!==i.is_deleted?{already_active:!0}:(await l(`\n    INSERT INTO lab_breeding_projects (\n      timestamp, id, name, code, project_type, status, primary_goal,\n      target_traits, traits_to_avoid, female_strain_id, male_strain_id,\n      female_plant_id, male_plant_id, started_at, target_completion, completed_at,\n      total_seeds_produced, seeds_tested, successful_phenos, notes, is_deleted\n    ) VALUES (\n      now(), '${e}', ${toSqlValue(i.name)}, ${toSqlValue(i.code)},\n      ${toSqlValue(i.project_type||"cross")}, ${toSqlValue(i.status||"planning")},\n      ${toSqlValue(i.primary_goal)}, ${toSqlValue(i.target_traits)},\n      ${toSqlValue(i.traits_to_avoid)}, ${toSqlValue(i.female_strain_id)},\n      ${toSqlValue(i.male_strain_id)}, ${toSqlValue(i.female_plant_id)}, ${toSqlValue(i.male_plant_id)},\n      ${toSqlValue(i.started_at)}, ${toSqlValue(i.target_completion)}, ${toSqlValue(i.completed_at)},\n      ${toSqlValue(null!=(t=i.total_seeds_produced)?t:0)}, ${toSqlValue(null!=(a=i.seeds_tested)?a:0)},\n      ${toSqlValue(null!=(o=i.successful_phenos)?o:0)}, ${toSqlValue(i.notes)}, 0\n    )\n  `),{success:!0})}(r)}}catch(e){if(e&&"object"==typeof e&&"statusCode"in e)throw e;throw console.error("Restore failed:",e),n({statusCode:500,message:"Failed to restore item"})}});export{_ as default};
