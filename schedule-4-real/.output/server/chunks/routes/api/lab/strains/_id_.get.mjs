import{d as t,x as e,c as n,R as s,q as o}from"../../../../nitro/nitro.mjs";import"node:http";import"node:https";import"node:crypto";import"stream";import"events";import"http";import"crypto";import"buffer";import"zlib";import"https";import"net";import"tls";import"url";import"node:events";import"node:buffer";import"node:fs";import"node:path";import"fs";import"path";import"mqtt";import"child_process";import"pg";import"fs/promises";import"node:url";const a=t(async t=>{const a=e(t,"id");if(!a)throw n({statusCode:400,message:"Strain ID is required"});let r;try{r=s(a)}catch{throw n({statusCode:400,message:"Invalid strain ID format"})}try{const t=await o(`\n      SELECT * FROM (\n        SELECT *, ROW_NUMBER() OVER (PARTITION BY id ORDER BY timestamp DESC) as rn\n        FROM lab_strains\n        WHERE id = '${r}'\n      )\n      WHERE rn = 1 AND is_deleted = 0\n    `);if(!t.length)throw n({statusCode:404,message:"Strain not found"});const e=t[0],s=await o(`\n      SELECT\n        id, plant_code, status, selection_status, pheno_label, is_keeper,\n        final_score, sex, origin_type, parent_plant_id, date_started, date_to_flower,\n        date_harvested, total_veg_days, total_flower_days, final_height_cm,\n        training_methods, container_size, grow_medium, keeper_notes\n      FROM (\n        SELECT *, ROW_NUMBER() OVER (PARTITION BY id ORDER BY timestamp DESC) as rn\n        FROM lab_plants\n        WHERE strain_id = '${r}'\n      )\n      WHERE rn = 1 AND is_deleted = 0\n      ORDER BY date_started DESC\n    `),a=s.map(t=>t.id),i=a.length>0?`'${a.join("','")}'`:"",[d,l,_]=await Promise.all([a.length>0?o(`\n            SELECT obs.photos, obs.observed_at, obs.plant_id, p.plant_code\n            FROM (\n              SELECT id, plant_id, photos, observed_at, is_deleted,\n                ROW_NUMBER() OVER (PARTITION BY id ORDER BY timestamp DESC) as rn\n              FROM lab_observations\n              WHERE plant_id IN (${i})\n            ) obs\n            LEFT JOIN (\n              SELECT id, plant_code, ROW_NUMBER() OVER (PARTITION BY id ORDER BY timestamp DESC) as rn\n              FROM lab_plants\n            ) p ON obs.plant_id = p.id AND p.rn = 1\n            WHERE obs.rn = 1 AND obs.is_deleted = 0 AND obs.photos IS NOT NULL\n            ORDER BY obs.observed_at DESC\n            LIMIT 50\n          `):Promise.resolve([]),a.length>0?o(`\n            SELECT obs.id, obs.plant_id, obs.observed_at, obs.obs_type, obs.general_notes,\n              obs.health_score, obs.stage, p.plant_code\n            FROM (\n              SELECT id, plant_id, observed_at, obs_type, general_notes, health_score, stage, is_deleted,\n                ROW_NUMBER() OVER (PARTITION BY id ORDER BY timestamp DESC) as rn\n              FROM lab_observations\n              WHERE plant_id IN (${i})\n            ) obs\n            LEFT JOIN (\n              SELECT id, plant_code, ROW_NUMBER() OVER (PARTITION BY id ORDER BY timestamp DESC) as rn\n              FROM lab_plants\n            ) p ON obs.plant_id = p.id AND p.rn = 1\n            WHERE obs.rn = 1 AND obs.is_deleted = 0\n            ORDER BY obs.observed_at DESC\n            LIMIT 10\n          `):Promise.resolve([]),o(`\n        SELECT id, name, code, project_type, status, primary_goal\n        FROM (\n          SELECT *, ROW_NUMBER() OVER (PARTITION BY id ORDER BY timestamp DESC) as rn\n          FROM lab_breeding_projects\n          WHERE female_strain_id = '${r}' OR male_strain_id = '${r}'\n        )\n        WHERE rn = 1 AND is_deleted = 0\n        ORDER BY timestamp DESC\n        LIMIT 10\n      `)]);let p=[];for(const t of d)if(t.photos)try{const e=JSON.parse(t.photos);if(Array.isArray(e))for(const n of e)p.push({url:n,plant_code:t.plant_code,plant_id:t.plant_id,observed_at:t.observed_at})}catch{}p=p.slice(0,20);const m=l,E=s.filter(t=>!["archived","culled"].includes(t.status)),c=s.filter(t=>["archived","harvest","drying","curing"].includes(t.status)),R=s.filter(t=>t.is_keeper),h=s.filter(t=>t.final_score&&t.final_score>0),f=h.length>0?h.reduce((t,e)=>t+e.final_score,0)/h.length:0,O=h.length>0?Math.max(...h.map(t=>t.final_score)):0,b=h.find(t=>t.final_score===O),g=s.filter(t=>t.total_flower_days&&t.total_flower_days>0),u=g.length>0?Math.round(g.reduce((t,e)=>t+e.total_flower_days,0)/g.length):null,N=s.filter(t=>t.final_height_cm&&t.final_height_cm>0),D=N.length>0?Math.round(N.reduce((t,e)=>t+e.final_height_cm,0)/N.length):null,T={};for(const t of s)if(t.training_methods)try{const e=JSON.parse(t.training_methods);for(const t of e)T[t]=(T[t]||0)+1}catch{}const S=Object.entries(T).sort((t,e)=>e[1]-t[1]).slice(0,3).map(([t])=>t),C={};for(const t of s)C[t.status]=(C[t.status]||0)+1;return{...e,dominant_terpenes:e.dominant_terpenes?JSON.parse(e.dominant_terpenes):[],plants:s.map(t=>({...t,training_methods:t.training_methods?JSON.parse(t.training_methods):[]})),photos:p,recent_observations:m,breeding_projects:_,stats:{total_plants:s.length,active_plants:E.length,completed_plants:c.length,keepers:R.length,avg_score:Math.round(10*f)/10,best_score:Math.round(10*O)/10,best_plant:b?{id:b.id,code:b.plant_code,score:b.final_score}:null,avg_flowering_days:u,flowering_diff:u&&e.expected_flowering_days?u-e.expected_flowering_days:null,avg_height_cm:D,common_training:S,plants_by_status:C}}}catch(t){if(t.statusCode)throw t;throw console.error("Failed to fetch strain:",t),n({statusCode:500,message:"Failed to fetch strain"})}});export{a as default};
