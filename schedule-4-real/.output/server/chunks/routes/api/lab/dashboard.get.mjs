import{d as t,q as n,O as e}from"../../../nitro/nitro.mjs";import"node:http";import"node:https";import"node:crypto";import"stream";import"events";import"http";import"crypto";import"buffer";import"zlib";import"https";import"net";import"tls";import"url";import"node:events";import"node:buffer";import"node:fs";import"node:path";import"fs";import"path";import"mqtt";import"child_process";import"pg";import"fs/promises";import"node:url";const s=t(async()=>{var t,s,i,r;try{const[a,o,E,R,d,l,p]=await Promise.all([n("\n        SELECT status, count() as cnt FROM (\n          SELECT status, is_deleted, ROW_NUMBER() OVER (PARTITION BY id ORDER BY timestamp DESC) as rn\n          FROM lab_plants\n        )\n        WHERE rn = 1 AND (is_deleted = 0 OR is_deleted IS NULL) AND status NOT IN ('archived', 'culled')\n        GROUP BY status\n      "),n("\n        SELECT count() as cnt FROM (\n          SELECT is_keeper, is_deleted, ROW_NUMBER() OVER (PARTITION BY id ORDER BY timestamp DESC) as rn\n          FROM lab_plants\n        )\n        WHERE rn = 1 AND (is_deleted = 0 OR is_deleted IS NULL) AND is_keeper = 1\n      "),n("\n        SELECT id, plant_code, pheno_label, strain_id, status, final_score, selection_status FROM (\n          SELECT *, ROW_NUMBER() OVER (PARTITION BY id ORDER BY timestamp DESC) as rn\n          FROM lab_plants\n        )\n        WHERE rn = 1 AND (is_deleted = 0 OR is_deleted IS NULL) AND final_score IS NOT NULL\n        ORDER BY final_score DESC\n        LIMIT 5\n      "),n("\n        SELECT count() as cnt FROM (\n          SELECT acknowledged, ROW_NUMBER() OVER (PARTITION BY id ORDER BY timestamp DESC) as rn\n          FROM lab_notifications\n        )\n        WHERE rn = 1 AND acknowledged = 0\n      "),n("\n        SELECT * FROM (\n          SELECT *, ROW_NUMBER() OVER (PARTITION BY id ORDER BY timestamp DESC) as rn\n          FROM lab_notifications\n        )\n        WHERE rn = 1 AND resolved = 0 AND level IN ('urgent', 'important')\n        ORDER BY timestamp DESC\n        LIMIT 10\n      "),n("\n        SELECT count() as cnt FROM (\n          SELECT id, is_deleted, ROW_NUMBER() OVER (PARTITION BY id ORDER BY timestamp DESC) as rn\n          FROM lab_strains\n        )\n        WHERE rn = 1 AND (is_deleted = 0 OR is_deleted IS NULL)\n      "),n("\n        SELECT count() as cnt FROM (\n          SELECT id, is_deleted, ROW_NUMBER() OVER (PARTITION BY id ORDER BY timestamp DESC) as rn\n          FROM lab_observations\n          WHERE timestamp > dateadd('d', -7, now())\n        )\n        WHERE rn = 1 AND (is_deleted = 0 OR is_deleted IS NULL)\n      ")]),m=a.reduce((t,n)=>t+parseInt(n.cnt),0),_=["pre_flower","early_flower","mid_flower","late_flower","flush"],O=a.filter(t=>_.includes(t.status)).reduce((t,n)=>t+parseInt(n.cnt),0),c=(null==(t=o[0])?void 0:t.cnt)||0,u=[...new Set(E.map(t=>t.strain_id).filter(Boolean))].map(t=>{try{return e(t)}catch{return null}}).filter(t=>null!==t);let N={};if(u.length>0){const t=await n(`\n        SELECT * FROM (\n          SELECT id, name, ROW_NUMBER() OVER (PARTITION BY id ORDER BY timestamp DESC) as rn\n          FROM lab_strains\n          WHERE id IN ('${u.join("','")}')\n        )\n        WHERE rn = 1\n      `);N=Object.fromEntries(t.map(t=>[t.id,t.name]))}const T=(null==(s=R[0])?void 0:s.cnt)||0,S=(null==(i=l[0])?void 0:i.cnt)||0;return{active_plants:m,flowering_count:O,keepers_count:c,strain_count:S,unread_notifications:T,recent_observations:(null==(r=p[0])?void 0:r.cnt)||0,status_distribution:Object.fromEntries(a.map(t=>[t.status,parseInt(t.cnt)])),top_performers:E.map(t=>({...t,strain_name:N[t.strain_id]||null})),urgent_alerts:d}}catch(t){return console.error("Failed to fetch dashboard stats:",t),{active_plants:0,flowering_count:0,keepers_count:0,strain_count:0,unread_notifications:0,recent_observations:0,status_distribution:{},top_performers:[],urgent_alerts:[]}}});export{s as default};
