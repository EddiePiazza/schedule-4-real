import{d as e,r as t,c as n}from"../../../nitro/nitro.mjs";import{query as a}from"../../../_/db.mjs";import{randomUUID as i}from"crypto";import"node:http";import"node:https";import"node:crypto";import"stream";import"events";import"http";import"buffer";import"zlib";import"https";import"net";import"tls";import"url";import"node:events";import"node:buffer";import"node:fs";import"node:path";import"fs";import"path";import"fs/promises";import"node:url";import"pg";const s=e(async e=>{var s,_,r,o,d,l,p,m,c,u,g,L,h,$,v,N,f;const E=await t(e);if(!E.plant_id)throw n({statusCode:400,message:"plant_id is required"});const b=i(),y=(new Date).toISOString();try{let e=E.env_snapshot_id||null;if(!e)try{const t=await a("\n        SELECT temp, humi, vpd, co2 FROM sensors_environment\n        ORDER BY timestamp DESC\n        LIMIT 1\n      ");if(t.length>0){e=i();const n=t[0],u=(await a("\n          SELECT\n            avg(temp) as temp_avg,\n            min(temp) as temp_min,\n            max(temp) as temp_max,\n            avg(humi) as humi_avg,\n            avg(vpd) as vpd_avg\n          FROM sensors_environment\n          WHERE timestamp > dateadd('h', -24, now())\n        "))[0]||{};await a(`\n          INSERT INTO lab_env_snapshots (\n            timestamp, id, observation_id, plant_id, zone_id,\n            temp_current, temp_avg_24h, temp_min_24h, temp_max_24h,\n            humi_current, humi_avg_24h, vpd_current, vpd_avg_24h, co2_current\n          ) VALUES (\n            now(), '${e}', '${b}', '${E.plant_id}', NULL,\n            ${null!=(s=n.temp)?s:"NULL"}, ${null!=(_=u.temp_avg)?_:"NULL"}, ${null!=(r=u.temp_min)?r:"NULL"}, ${null!=(o=u.temp_max)?o:"NULL"},\n            ${null!=(d=n.humi)?d:"NULL"}, ${null!=(l=u.humi_avg)?l:"NULL"}, ${null!=(p=n.vpd)?p:"NULL"}, ${null!=(m=u.vpd_avg)?m:"NULL"}, ${null!=(c=n.co2)?c:"NULL"}\n          )\n        `)}}catch(e){}if(await a(`\n      INSERT INTO lab_observations (\n        timestamp, id, plant_id, observed_at, week_number, day_number, stage, obs_type,\n        height_cm, health_score, vigor_score, pest_issues, pest_details,\n        disease_issues, disease_details, deficiency_issues, deficiency_details,\n        watering_notes, feeding_notes, training_applied, general_notes,\n        photos, checklist_data, diagnostic_data, activity_data, env_snapshot_id, batch_id, is_deleted\n      ) VALUES (\n        now(),\n        '${b}',\n        '${E.plant_id}',\n        '${E.observed_at||y.split("T")[0]}',\n        ${null!=(u=E.week_number)?u:"NULL"},\n        ${null!=(g=E.day_number)?g:"NULL"},\n        ${E.stage?`'${E.stage}'`:"NULL"},\n        '${E.obs_type||"general"}',\n        ${null!=(L=E.height_cm)?L:"NULL"},\n        ${null!=(h=E.health_score)?h:"NULL"},\n        ${null!=($=E.vigor_score)?$:"NULL"},\n        ${E.pest_issues?1:0},\n        ${E.pest_details?`'${E.pest_details.replace(/'/g,"''")}'`:"NULL"},\n        ${E.disease_issues?1:0},\n        ${E.disease_details?`'${E.disease_details.replace(/'/g,"''")}'`:"NULL"},\n        ${E.deficiency_issues?1:0},\n        ${E.deficiency_details?`'${E.deficiency_details.replace(/'/g,"''")}'`:"NULL"},\n        ${E.watering_notes?`'${E.watering_notes.replace(/'/g,"''")}'`:"NULL"},\n        ${E.feeding_notes?`'${E.feeding_notes.replace(/'/g,"''")}'`:"NULL"},\n        ${E.training_applied?`'${JSON.stringify(E.training_applied)}'`:"NULL"},\n        ${E.general_notes?`'${E.general_notes.replace(/'/g,"''")}'`:"NULL"},\n        ${E.photos?`'${JSON.stringify(E.photos)}'`:"NULL"},\n        ${E.checklist_data?`'${JSON.stringify(E.checklist_data)}'`:"NULL"},\n        ${E.diagnostic_data?`'${JSON.stringify(E.diagnostic_data).replace(/'/g,"''")}'`:"NULL"},\n        ${E.activity_data?`'${JSON.stringify(E.activity_data).replace(/'/g,"''")}'`:"NULL"},\n        ${e?`'${e}'`:"NULL"},\n        ${E.batch_id?`'${E.batch_id}'`:"NULL"},\n        0\n      )\n    `),E.week_number||E.day_number){const e=await a(`\n        SELECT * FROM (\n          SELECT *, ROW_NUMBER() OVER (PARTITION BY id ORDER BY timestamp DESC) as rn\n          FROM lab_plants\n          WHERE id = '${E.plant_id}' AND is_deleted = 0\n        )\n        WHERE rn = 1\n      `);if(e.length>0){e[0];await a(`\n          INSERT INTO lab_plants (\n            timestamp, id, strain_id, seed_batch_id, zone_id, plant_code, pheno_label,\n            origin_type, parent_plant_id, project_goal, status, selection_status,\n            cull_reason, cull_date, is_keeper, keeper_notes, sex, sex_confirmed_date,\n            date_started, date_to_veg, date_to_flower, date_harvested,\n            current_week, current_day, total_veg_days, total_flower_days,\n            container_size, grow_medium, nutrients_type, training_methods,\n            final_height_cm, final_score, hof_category, hof_notes,\n            breeding_project_id, hunt_id, keeper_tier, run_count, is_deleted\n          )\n          SELECT now(), id, strain_id, seed_batch_id, zone_id, plant_code, pheno_label,\n            origin_type, parent_plant_id, project_goal, status, selection_status,\n            cull_reason, cull_date, is_keeper, keeper_notes, sex, sex_confirmed_date,\n            date_started, date_to_veg, date_to_flower, date_harvested,\n            ${null!=(v=E.week_number)?v:"current_week"}, ${null!=(N=E.day_number)?N:"current_day"}, total_veg_days, total_flower_days,\n            container_size, grow_medium, nutrients_type, training_methods,\n            ${null!=(f=E.height_cm)?f:"final_height_cm"}, final_score, hof_category, hof_notes,\n            breeding_project_id, hunt_id, keeper_tier, run_count, is_deleted\n          FROM lab_plants\n          WHERE id = '${E.plant_id}'\n          LATEST ON timestamp PARTITION BY id\n        `)}}const t=await a(`\n      SELECT * FROM lab_observations\n      WHERE id = '${b}'\n      ORDER BY timestamp DESC\n      LIMIT 1\n    `);let n=null;if(e){n=(await a(`\n        SELECT * FROM lab_env_snapshots WHERE id = '${e}' LIMIT 1\n      `))[0]||null}return{...t[0],env_snapshot:n}}catch(e){throw console.error("Failed to create observation:",e),n({statusCode:500,message:"Failed to create observation"})}});export{s as default};
