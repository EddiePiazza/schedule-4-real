import{d as t,a as e,C as i,s as n,q as s}from"../../../nitro/nitro.mjs";import"node:http";import"node:https";import"node:crypto";import"stream";import"events";import"http";import"crypto";import"buffer";import"zlib";import"https";import"net";import"tls";import"url";import"node:events";import"node:buffer";import"node:fs";import"node:path";import"fs";import"path";import"mqtt";import"child_process";import"pg";import"fs/promises";import"node:url";const o=t(async t=>{var o,r;const a=e(t),m=a.status,p=a.selection,d=Math.min(i(a.limit||"50"),500),l=Math.max(0,i(a.offset||"0"));try{let t="rn = 1 AND is_deleted = 0";m&&(t+="active"===m?" AND status NOT IN ('archived', 'culled')":` AND status = '${n(m)}'`),p&&(t+=` AND selection_status = '${n(p)}'`);const e=(await s(`\n      SELECT * FROM (\n        SELECT *, ROW_NUMBER() OVER (PARTITION BY id ORDER BY timestamp DESC) as rn\n        FROM lab_plants\n      )\n      WHERE ${t}\n      ORDER BY date_started DESC\n      LIMIT ${d+l}\n    `)).slice(l,l+d),i=[...new Set(e.map(t=>t.strain_id).filter(Boolean))];let o={};if(i.length>0)try{const t=await s(`\n          SELECT id, name FROM (\n            SELECT id, name, is_deleted, ROW_NUMBER() OVER (PARTITION BY id ORDER BY timestamp DESC) as rn\n            FROM lab_strains\n            WHERE id IN ('${i.join("','")}')\n          )\n          WHERE rn = 1 AND is_deleted = 0\n        `);o=Object.fromEntries(t.map(t=>[t.id,t.name]))}catch{}return e.map(t=>({...t,strain_name:o[t.strain_id]||null,training_methods:t.training_methods?JSON.parse(t.training_methods):[]}))}catch(t){return(null==(o=t.message)?void 0:o.includes("does not exist"))||(null==(r=t.message)?void 0:r.includes("table not found"))||console.error("Failed to fetch plants:",t),[]}});export{o as default};
