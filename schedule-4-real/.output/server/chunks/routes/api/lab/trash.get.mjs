import{d as e,a as t,q as n}from"../../../nitro/nitro.mjs";import"node:http";import"node:https";import"node:crypto";import"stream";import"events";import"http";import"crypto";import"buffer";import"zlib";import"https";import"net";import"tls";import"url";import"node:events";import"node:buffer";import"node:fs";import"node:path";import"fs";import"path";import"mqtt";import"child_process";import"pg";import"fs/promises";import"node:url";const s=new Set(["plants","strains","observations","harvests","pollen","hunts","breeding"]),i=e(async e=>{const i=t(e),a="string"==typeof i.type&&s.has(i.type)?i.type:null,r=Math.max(1,Math.min(200,Number(i.limit)||50));async function safeQuery(e){try{return await n(e)}catch(e){return e instanceof Error&&/does not exist|table not found/i.test(e.message)||console.error("trash query failed:",e),[]}}const wants=e=>!a||a===e,[o,m,d,p,l,E,R]=await Promise.all([wants("plants")?safeQuery(`\n      SELECT id, plant_code, pheno_label, timestamp FROM (\n        SELECT *, ROW_NUMBER() OVER (PARTITION BY id ORDER BY timestamp DESC) as rn\n        FROM lab_plants\n      )\n      WHERE rn = 1 AND is_deleted = 1\n      ORDER BY timestamp DESC\n      LIMIT ${r}\n    `):Promise.resolve([]),wants("strains")?safeQuery(`\n      SELECT id, name, breeder, timestamp FROM (\n        SELECT *, ROW_NUMBER() OVER (PARTITION BY id ORDER BY timestamp DESC) as rn\n        FROM lab_strains\n      )\n      WHERE rn = 1 AND is_deleted = 1\n      ORDER BY timestamp DESC\n      LIMIT ${r}\n    `):Promise.resolve([]),wants("observations")?safeQuery(`\n      SELECT id, plant_id, obs_type, observed_at, timestamp FROM (\n        SELECT *, ROW_NUMBER() OVER (PARTITION BY id ORDER BY timestamp DESC) as rn\n        FROM lab_observations\n      )\n      WHERE rn = 1 AND is_deleted = 1\n      ORDER BY timestamp DESC\n      LIMIT ${r}\n    `):Promise.resolve([]),wants("harvests")?safeQuery(`\n      SELECT id, plant_id, harvest_date, notes, is_deleted, timestamp FROM (\n        SELECT *, ROW_NUMBER() OVER (PARTITION BY id ORDER BY timestamp DESC) as rn\n        FROM lab_harvest_data\n      )\n      WHERE rn = 1 AND (is_deleted = 1 OR notes = '__DELETED__')\n      ORDER BY timestamp DESC\n      LIMIT ${r}\n    `):Promise.resolve([]),wants("pollen")?safeQuery(`\n      SELECT id, batch_code, strain_id, status, is_deleted, timestamp FROM (\n        SELECT *, ROW_NUMBER() OVER (PARTITION BY id ORDER BY timestamp DESC) as rn\n        FROM lab_pollen_storage\n      )\n      WHERE rn = 1 AND (is_deleted = 1 OR status = 'depleted')\n      ORDER BY timestamp DESC\n      LIMIT ${r}\n    `):Promise.resolve([]),wants("hunts")?safeQuery(`\n      SELECT id, name, strain_id, timestamp FROM (\n        SELECT *, ROW_NUMBER() OVER (PARTITION BY id ORDER BY timestamp DESC) as rn\n        FROM lab_hunts\n      )\n      WHERE rn = 1 AND is_deleted = 1\n      ORDER BY timestamp DESC\n      LIMIT ${r}\n    `):Promise.resolve([]),wants("breeding")?safeQuery(`\n      SELECT id, name, code, timestamp FROM (\n        SELECT *, ROW_NUMBER() OVER (PARTITION BY id ORDER BY timestamp DESC) as rn\n        FROM lab_breeding_projects\n      )\n      WHERE rn = 1 AND is_deleted = 1\n      ORDER BY timestamp DESC\n      LIMIT ${r}\n    `):Promise.resolve([])]),_=[...o.map(e=>({type:"plant",id:e.id,label:e.plant_code||"Unknown plant",subtitle:e.pheno_label||void 0,deleted_at:e.timestamp})),...m.map(e=>({type:"strain",id:e.id,label:e.name||"Unknown strain",subtitle:e.breeder||void 0,deleted_at:e.timestamp})),...d.map(e=>({type:"observation",id:e.id,label:e.obs_type?`${e.obs_type} observation`:"Observation",subtitle:e.observed_at||void 0,deleted_at:e.timestamp})),...p.map(e=>({type:"harvest",id:e.id,label:"Harvest",subtitle:e.harvest_date||void 0,deleted_at:e.timestamp})),...l.map(e=>({type:"pollen",id:e.id,label:e.batch_code||"Pollen batch",subtitle:e.status||void 0,deleted_at:e.timestamp})),...E.map(e=>({type:"hunt",id:e.id,label:e.name||"Hunt",subtitle:void 0,deleted_at:e.timestamp})),...R.map(e=>({type:"breeding_project",id:e.id,label:e.name||"Breeding project",subtitle:e.code||void 0,deleted_at:e.timestamp}))];return _.sort((e,t)=>{const n=Number(e.deleted_at)||0;return(Number(t.deleted_at)||0)-n}),{items:_,count:_.length}});export{i as default};
