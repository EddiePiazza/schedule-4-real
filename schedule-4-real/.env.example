# Schedule 4 Real — App Configuration

# MQTT Local Broker (Mosquitto from hybrid proxy)
MQTT_HOST=127.0.0.1
MQTT_PORT=1883
MQTT_LAN_PORT=1884

# MQTT Proxy (TLS MITM - fixed at 8883, compiled into spiderproxy binary)
PROXY_PORT=8883

# QuestDB Configuration
# Use 'questdb' for Docker, '127.0.0.1' for local development
QUESTDB_HOST=127.0.0.1
QUESTDB_PG_PORT=8812
QUESTDB_HTTP_PORT=9000
QUESTDB_ILP_PORT=9009
QUESTDB_USER=spider
QUESTDB_PASSWORD=spider123
QUESTDB_DATABASE=qdb

# Data Retention (days)
DATA_RETENTION_DAYS=90

# API Server
API_PORT=3000
WS_PORT=3001

# App Password (required for all save/POST operations)
APP_PASSWORD=spiderdream
